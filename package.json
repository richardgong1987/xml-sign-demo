{
  "name": "saml-sso-demo",
  "version": "1.0.0",
  "private": true,
  "description": "A SAML 2.0 SSO demo: a NestJS identity provider and a Next.js service provider, cooperating over HTTP.",
  "workspaces": [
    "apps/*",
    "e2e"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "start:idp": "npm run start --workspace @saml-demo/identity-provider",
    "start:sp": "npm run start --workspace @saml-demo/service-provider",
    "dev:idp": "npm run start:dev --workspace @saml-demo/identity-provider",
    "dev:sp": "npm run dev --workspace @saml-demo/service-provider",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "npm run test --workspace @saml-demo/identity-provider && npm run test --workspace @saml-demo/service-provider",
    "test:e2e": "npm run test --workspace @saml-demo/e2e",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=24"
  }
}
