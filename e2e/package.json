{
  "name": "@saml-demo/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-service end-to-end tests: the only code that runs both applications at once.",
  "scripts": {
    "test": "jest --runInBand",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.2",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "typescript": "^5.9.3"
  }
}
